import React from 'react';
import Formulaire from '../component/Formulaire';

function Login() {

  return (
    <div className="Login">
        <h1>Login</h1>
        <Formulaire />
    </div>
  );
}

export default Login;
